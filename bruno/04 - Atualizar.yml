info:
  name: 04 - Atualizar
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/clubes/3"
  body:
    type: json
    data: |-
      {
        "nome": "Sociedade Esportiva Palmeiras",
        "estado": "SP",
        "serieA": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
